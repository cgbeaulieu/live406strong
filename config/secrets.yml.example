# Optional: set GA_MEASUREMENT_ID (or credentials ga4_measurement_id) for GA4 — see ApplicationHelper#ga4_measurement_id.
development:
  secret_key_base: replace-with-local-dev-secret

test:
  secret_key_base: replace-with-local-test-secret

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
